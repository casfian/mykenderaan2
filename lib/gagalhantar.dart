import 'package:flutter/material.dart';

class GagalHantar extends StatelessWidget {
  const GagalHantar({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Gagal Hantar'),
      ),
      body: const Center(
        child: Text('Gagal Hantar'),
      ),
    );
  }
}
