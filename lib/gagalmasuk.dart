import 'package:flutter/material.dart';

class GagalMasuk extends StatelessWidget {
  const GagalMasuk({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Gagal Masuk'),
      ),
      body: const Center(
        child: Text('Gagal Masuk'),
      ),
    );
  }
}
